image: debian/stable
packages:
  - build-essential
  - autoconf
  - libtool
sources:
  - https://hg.sr.ht/~m6kvm/libhoratio
environment:
  deploy: palmer@overchat.org
tasks:
  - setup: |
      cd libhoratio
  - build: |
      cd libhoratio
      debian/rules
      make && make check
